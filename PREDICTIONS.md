1. Double: I believe the function I wrote is pretty easy to understand, it just takes the user input (n), adds it to itself, and returns the result.

2. Square: This function will square n, it is quite simple as it just returns n * n.

